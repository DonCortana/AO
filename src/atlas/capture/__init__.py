"""Layer B capture configuration — the facts a consumer-surface session must
establish about itself before anything it produces is treated as evidence.

Distinct from `atlas.tools.consumer_ingest`, which is the other end of the
same pipeline: ingest reads a completed capture sheet, this package governs
the session that produces one.
"""
